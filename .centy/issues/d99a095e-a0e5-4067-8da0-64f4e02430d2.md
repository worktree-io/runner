---
# This file is managed by Centy. Use the Centy CLI to modify it.
displayNumber: 50
status: closed
priority: 2
createdAt: 2026-03-19T06:48:16.982092+00:00
updatedAt: 2026-03-19T06:56:49.560706+00:00
---

# test: achieve 100% coverage for opener/detect.rs

The \`src/opener/detect.rs\` module currently has untested paths that prevent 100% line coverage and may be excluded from the pre-push coverage check.

## Goal

Achieve 100% line coverage for \`src/opener/detect.rs\` so the file can be removed from the \`--ignore-filename-regex\` exclusion list in the pre-push hook.

## Coverage Gaps

- \`DetectMethod::MacosApp\` arm in \`is_available()\` — only reachable on macOS, and only when a \`.app\` bundle actually exists or does not exist in \`/Applications\` or \`~/Applications\`
- \`macos_app_exists()\` — tests must cover both the system path (\`/Applications/<app>.app\`) and the user path (\`~/Applications/<app>.app\`) existing/not-existing, plus the \`dirs::home_dir()\` returning \`None\` edge case

## Pre-push Hook Change

The \`.cargo-husky/hooks/pre-push\` script excludes files via the \`IGNORE\` regex (line 41). Once coverage is complete, remove \`opener/detect\` from the pattern:

\`\`\`
# before
opener/(mod|terminal)
# after — detect no longer excluded
opener/(mod|terminal)   ← remove |detect if it was added, or leave as-is
\`\`\`

After editing the hook file, copy it to \`.git/hooks/pre-push\` to apply immediately:

\`\`\`sh
cp .cargo-husky/hooks/pre-push .git/hooks/pre-push
\`\`\`

## Acceptance Criteria

- All lines in \`src/opener/detect.rs\` are covered (no lines excluded via \`LLVM_COV_EXCL_LINE\` or filename regex)
- \`cargo llvm-cov --summary-only\` reports 100% line coverage for the file
- Tests use \`#[cfg(target_os = "macos")]\` guards where platform-specific behavior is tested
- \`opener/detect\` is **not** present in the \`IGNORE\` regex in \`.cargo-husky/hooks/pre-push\`
- Pre-push hook passes without needing to exclude this file
